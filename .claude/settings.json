{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Grep|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Verwende stattdessen die vectorgrep MCP Tools (search_code, search_files, search_symbols)' >&2; exit 2"
          }
        ]
      },
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -qi '\"subagent_type\".*\"Explore\"'; then echo 'Verwende vectorgrep MCP Tools statt Agent Explore für Codebase-Suchen' >&2; exit 2; fi; exit 0"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -qE '\"command\".*\\b(grep|rg)\\b'; then echo 'Verwende vectorgrep MCP Tools statt grep/rg im Terminal' >&2; exit 2; fi; exit 0"
          }
        ]
      }
    ]
  }
}
