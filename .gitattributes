# Exclude from source archives (git archive / GitHub releases)

# Development files
.github/ export-ignore
.claude/ export-ignore
*.log export-ignore

# Shell scripts must use LF line endings (CRLF breaks the shebang on Linux)
*.sh text eol=lf
