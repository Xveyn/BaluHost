/**
 * Devices Page
 *
 * Unified device management view showing all devices (mobile and desktop)
 * with QR registration and sync schedules.
 */

import DeviceManagement from './DeviceManagement';

export default function DevicesPage() {
  return <DeviceManagement />;
}
