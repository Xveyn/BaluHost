# Development / optional requirements for backend
# Install dev tools:
# pip install -e .[dev]

# Optional scheduler extra (APScheduler) for production scheduler support
# Install via pip extras from the repository root:
# pip install -e .[scheduler]
apscheduler>=3.10.0,<4.0.0

# Optional testing tools (also available via the 'dev' extra in pyproject)
pytest>=9.0.3,<10.0.0
pytest-asyncio>=1.0.0,<2.0.0
httpx>=0.27.0,<0.28.0